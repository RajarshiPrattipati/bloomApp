{
  "name": "bloom-gate",
  "version": "0.1.0",
  "private": true,
  "description": "BLOOM greenlight-gate vertical slice — server-authoritative spin loop",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c green,cyan \"npm -w server run dev\" \"npm -w client run dev\"",
    "dev:server": "npm -w server run dev",
    "dev:client": "npm -w client run dev",
    "smoke": "node scripts/smoke.mjs",
    "sim": "node scripts/sim.mjs",
    "e2e": "playwright test",
    "verdict": "node scripts/verdict.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "concurrently": "^9.1.2"
  }
}
